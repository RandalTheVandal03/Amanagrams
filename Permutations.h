#pragma once
#include <iostream>
#include <vector>
#include <string>
#include <set>

using namespace std;

class Permutations {

public:
	vector<vector<string>> permutations(vector<string> letters);
	vector<vector<string>> getPermutations();

};